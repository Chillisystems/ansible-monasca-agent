init_config:

instances:
    - consumer_groups:
        {% for k, v in monasca_plugin_kafka_consumer_groups.iteritems() %}
            '1_{{ k }}': '{{ k }}': {{ v | to_json }}
        {% endfor %}

      kafka_connect_str: localhost:9092
      zk_connect_str: localhost:2181
