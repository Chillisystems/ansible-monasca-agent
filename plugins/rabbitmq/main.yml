---

- name: MONA | Create rabbitmq plugin configuration file
  template:
   dest: '{{ agent_plugin_conf_dir }}/rabbitmq.yaml'
   owner: '{{ monasca_agent_user }}'
   group: '{{ monasca_group }}'
   mode: 0640
   src: plugins/rabbitmq/rabbitmq.yaml.j2
  notify:
    - restart monasca-agent
